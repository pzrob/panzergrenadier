{game_entity
	(include "/properties/marine.ext")
	(include "/properties/selection/vehicle_big.inc" scale(1.3))
	{props "marine" "open_without_land" "emit_on_sink" "multi_board" "explosion_throw_crew" "car"}
 	{targetSelector "tank_bullet"}
	{patherid "shallow_water_0"}
	{extension "lca.mdl"}
	(include "lca.ext")
	{extension "crew.ext"}

	{extender "enumerator"
		{digit_folder "us2"}
	}

	{extender "inventory"
		{weapon "mgun" {mask "mountable"}}
		{box
			{item "mg34_belt" filled {user "mgun"}}
			{item "mgun_ger belt" 2010}
		}
	}

	{Weaponry
;		("restore_ik_time")
		{place "mgun"
			{type "hull"}
			{gunner "gunner"}
			{charger "gunner"}
			{LinkBone "mgun_rot"}
			{LinkAnimation "off"}
			("abm_mgun")
			{basic}
			{ai_in_manual}
		}
	}

	{mass 9144}
	{chassis
		{Locomotion ("locomotion")
			{TurnRadius	4}
			{MaxSpeed	19}
			{StartTime	18}
			{BrakeTime	6}
			{TurnTime	45}
		}
	}
}
